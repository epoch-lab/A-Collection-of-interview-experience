自我介绍
实习工作中遇到的难点
vue和react的区别
响应式是如何实现的
git用过哪些命令
git rebase和git merge
工作中用过ts吗
ts的特性
ts如何对类型进行声明
never和void的区别
any和unknow的区别
==和===的区别
var let const的区别
如果用const定义一个obj，能对obj进行修改吗
如何把ts转换为js
bable的原理
手撕：await 每隔一秒打印hello world

```js
async function sleep(ms){
    return new Promise((resolve)=>setTimeout(reslove, ms));
}

async function printHelloWorld(){
    while(true){
        await sleep(1000);
        console.log("hello world");
    }
}
```